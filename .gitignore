data/*
noshare*
logs*
wandb* 
*_dice*_normal_seed_*

*__pycache__
*.pyc
*.pyo
*.pyd
*.pkl
*.db
*.sqlite3
*.log

*/lightning_logs/*
*/models/*

# Local path configuration; .env.example IS committed as the template.
.env

# Local working copies fetched from the artifact archive (see ARTIFACTS.md).
/checkpoints/
/data/
/.venv/

# Nothing large is committed. Metrics CSVs, rendered figures, archive-quality
# videos and prediction caches all live in the artifact archive and are fetched
# with scripts/fetch_artifacts.sh. The only committed media is docs/videos/,
# the small demonstration loops.
/results/